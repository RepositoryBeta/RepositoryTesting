texto 90
I llegó acá pero aquí dice lo siguiente I que tiene 100 + 1 es 101, ese 101 guárdelo en ti mismo y sale de él mientras y regresa evaluar la condición 101, es menor o igual que 100 ya no entra a ejecutar las líneas del ciclo, sino que va a salir a estar la línea siguiente del fin, mientras que ya es el fin del proceso, entonces ahora podemos esto ejecutarlo y podemos apreciar que allí tenemos los números generados automáticamente, no tenemos nuestro diagrama de flujo, analizaré, ya saben que tiene inicio y tiene fin, hemos definido aquí ahí y le hemos asignado uno mientras y el ciclo para automáticamente no se incrementa el valor.